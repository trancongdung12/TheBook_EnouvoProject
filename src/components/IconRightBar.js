import React from 'react';
import { View, Text, TouchableOpacity } from 'react-native';

const IconRightBar = () => {
  return (
    <View>
      <TouchableOpacity>123</TouchableOpacity>
    </View>
  );
};

export default IconRightBar;
